<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mysmartshop.mapper.ProductMapper">

    <!-- 动态条件查询 -->
    <select id="selectByCondition" resultType="com.example.mysmartshop.entity.Product">
        SELECT p.*, c.name as categoryName
        FROM product p
        LEFT JOIN category c ON p.category_id = c.id
        <where>
            <if test="categoryId != null">
                AND p.category_id = #{categoryId}
            </if>
            <if test="name != null and name != ''">
                AND p.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="minPrice != null">
                AND p.price &gt;= #{minPrice}
            </if>
            <if test="maxPrice != null">
                AND p.price &lt;= #{maxPrice}
            </if>
        </where>
        ORDER BY p.id
    </select>

    <!-- 根据ID查询 -->
    <select id="selectById" resultType="com.example.mysmartshop.entity.Product">
        SELECT p.*, c.name as categoryName
        FROM product p
                 LEFT JOIN category c ON p.category_id = c.id
        WHERE p.id = #{id}
    </select>

    <!-- 插入 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product(name, category_id, price, stock, image_url, description, status)
        VALUES(#{name}, #{categoryId}, #{price}, #{stock}, #{imageUrl}, #{description}, #{status})
    </insert>

    <!-- 更新 -->
    <update id="update">
        UPDATE product
        SET name=#{name}, category_id=#{categoryId}, price=#{price},
            stock=#{stock}, image_url=#{imageUrl}, description=#{description}, status=#{status}
        WHERE id=#{id}
    </update>

    <!-- 删除 -->
    <delete id="delete">
        DELETE FROM product WHERE id=#{id}
    </delete>

</mapper>